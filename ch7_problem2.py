# WAP to greet all the person names stored in a list "l" and which starts with S

l = ["Sumit", "Elvish ", "Virat", "ABD"]

for name in l:
    
    
    if(name.startswith("S")):
        
        
        print(f"Hi {name}")
